<!DOCTYPE html>
<html>
<head>
	<title>YelpCamp</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
	<link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
</head>
<body>

	<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
      <a class="navbar-brand" href="/">YelpCamp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link <%= typeof page !== 'undefined' && page === 'campgrounds' ? 'active' : '' %>" href="/campgrounds">Home <span class="sr-only">(current)</span></a>
          </li>
          
        </ul>
     </div>
        
		  <div class="collapse navbar-collapse flex-grow-0" id="navbarCollapse">
			<div class="navbar-nav mr-auto "> <!-- OR set margin-left-auto on this <div> -->
				<%if(!currentUser){%>
			    	<li><a class="nav-item nav-link <%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>" href="/register">Sign up</a></li>
			    	<a class="nav-item nav-link <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>" href="/login">Login<span class="sr-only">(current)</span></a>
			    <%}else{%> 	
			    	<% var name=currentUser.username%>			    	
			    	<% if(currentUser.isAdmin){%>
			    		<%name="Admin";%>
			    	<%}%>

			    	<a class="nav-item nav-link" href="/user/<%=currentUser._id%>"><strong><%=name%></strong></a>
			  		<a class="nav-item nav-link" href="/logout">Logout</a>
			  	<%}%>	
			</div>	
		</div> 
    </nav>




	<div class="container">
		<%if(error&&error.length>0){%>
			<div class="alert alert-danger" role="alert">
				  <%=error%>
			</div>
		<%}%>
		<%if(success&&success.length>0){%>
			<div class="alert alert-success" role="alert">
				  <%=success%>
			</div>
		<%}%>
	</div>